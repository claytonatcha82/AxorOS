import type { Pool } from 'pg';

export interface ClientRecord { id: string; displayName: string; legalName: string | null; status: string; primaryEmail: string | null; primaryPhone: string | null; createdAt: string; updatedAt: string; }
export interface CreateClientInput { displayName: string; legalName?: string; primaryEmail?: string; primaryPhone?: string; }
export interface LeadRecord { id: string; clientId: string | null; companyName: string; contactName: string | null; contactEmail: string | null; source: string | null; opportunitySummary: string | null; leadScore: number | null; status: string; evidence: unknown; createdAt: string; updatedAt: string; }
export interface CreateLeadInput { companyName: string; contactName?: string; contactEmail?: string; source?: string; opportunitySummary?: string; leadScore?: number; evidence?: unknown; }
export interface EnrichLeadInput { companyName: string; opportunitySummary?: string; evidence: unknown; }
export interface LeadSourceIdentityRecord { provider: string; externalId: string; leadId: string; createdAt: string; refreshedAt: string; }
export interface PreliminaryLeadQualificationRecord { id: string; leadId: string; totalScore: number | null; suggestedStatus: string; humanReviewRequired: true; assessments: unknown; missingInformation: unknown; atlasSourcePaths: unknown; actorId: string; createdAt: string; }
export interface CreatePreliminaryLeadQualificationInput { leadId: string; totalScore: number | null; suggestedStatus: 'excellent' | 'good' | 'moderate' | 'poor_fit' | 'insufficient_information'; assessments: unknown; missingInformation: string[]; atlasSourcePaths: string[]; actorId?: string; }
export interface ProjectRecord { id: string; clientId: string; leadId: string | null; name: string; status: string; serviceType: string; createdAt: string; updatedAt: string; }
export interface CreateProjectInput { clientId: string; leadId?: string; name: string; serviceType?: string; }
export interface WorkflowEventRecord { id: string; clientId: string | null; projectId: string | null; eventType: string; actorType: string; actorId: string | null; payload: unknown; createdAt: string; }
export interface CreateWorkflowEventInput { clientId?: string; projectId?: string; eventType: string; actorType: 'founder' | 'agent' | 'system' | 'client' | 'provider'; actorId?: string; payload?: unknown; }

function mapClient(row: Record<string, unknown>): ClientRecord { return { id: String(row.id), displayName: String(row.display_name), legalName: row.legal_name === null ? null : String(row.legal_name), status: String(row.status), primaryEmail: row.primary_email === null ? null : String(row.primary_email), primaryPhone: row.primary_phone === null ? null : String(row.primary_phone), createdAt: new Date(String(row.created_at)).toISOString(), updatedAt: new Date(String(row.updated_at)).toISOString() }; }
function mapLead(row: Record<string, unknown>): LeadRecord { return { id: String(row.id), clientId: row.client_id === null ? null : String(row.client_id), companyName: String(row.company_name), contactName: row.contact_name === null ? null : String(row.contact_name), contactEmail: row.contact_email === null ? null : String(row.contact_email), source: row.source === null ? null : String(row.source), opportunitySummary: row.opportunity_summary === null ? null : String(row.opportunity_summary), leadScore: row.lead_score === null ? null : Number(row.lead_score), status: String(row.status), evidence: row.evidence, createdAt: new Date(String(row.created_at)).toISOString(), updatedAt: new Date(String(row.updated_at)).toISOString() }; }
function mapLeadSourceIdentity(row: Record<string, unknown>): LeadSourceIdentityRecord { return { provider: String(row.provider), externalId: String(row.external_id), leadId: String(row.lead_id), createdAt: new Date(String(row.created_at)).toISOString(), refreshedAt: new Date(String(row.refreshed_at)).toISOString() }; }
function mapPreliminaryLeadQualification(row: Record<string, unknown>): PreliminaryLeadQualificationRecord { return { id: String(row.id), leadId: String(row.lead_id), totalScore: row.total_score === null ? null : Number(row.total_score), suggestedStatus: String(row.suggested_status), humanReviewRequired: true, assessments: row.assessments, missingInformation: row.missing_information, atlasSourcePaths: row.atlas_source_paths, actorId: String(row.actor_id), createdAt: new Date(String(row.created_at)).toISOString() }; }
function mapProject(row: Record<string, unknown>): ProjectRecord { return { id: String(row.id), clientId: String(row.client_id), leadId: row.lead_id === null ? null : String(row.lead_id), name: String(row.name), status: String(row.status), serviceType: String(row.service_type), createdAt: new Date(String(row.created_at)).toISOString(), updatedAt: new Date(String(row.updated_at)).toISOString() }; }
function mapWorkflowEvent(row: Record<string, unknown>): WorkflowEventRecord { return { id: String(row.id), clientId: row.client_id === null ? null : String(row.client_id), projectId: row.project_id === null ? null : String(row.project_id), eventType: String(row.event_type), actorType: String(row.actor_type), actorId: row.actor_id === null ? null : String(row.actor_id), payload: row.payload, createdAt: new Date(String(row.created_at)).toISOString() }; }

const leadColumns = `id, client_id, company_name, contact_name, contact_email, source, opportunity_summary, lead_score, status, evidence, created_at, updated_at`;
const workflowEventColumns = `id, client_id, project_id, event_type, actor_type, actor_id, payload, created_at`;

export function createOperationalRepository(pool: Pool) {
  return {
    async listClients(limit = 50): Promise<ClientRecord[]> { const safeLimit = Math.max(1, Math.min(limit, 100)); const result = await pool.query('select id, display_name, legal_name, status, primary_email, primary_phone, created_at, updated_at from operational.clients order by created_at desc limit $1', [safeLimit]); return result.rows.map((row) => mapClient(row as Record<string, unknown>)); },
    async createClient(input: CreateClientInput): Promise<ClientRecord> { const result = await pool.query(`insert into operational.clients (display_name, legal_name, primary_email, primary_phone) values ($1, $2, $3, $4) returning id, display_name, legal_name, status, primary_email, primary_phone, created_at, updated_at`, [input.displayName.trim(), input.legalName?.trim() || null, input.primaryEmail?.trim() || null, input.primaryPhone?.trim() || null]); return mapClient(result.rows[0] as Record<string, unknown>); },
    async listLeads(limit = 50): Promise<LeadRecord[]> { const safeLimit = Math.max(1, Math.min(limit, 100)); const result = await pool.query(`select ${leadColumns} from operational.leads order by created_at desc limit $1`, [safeLimit]); return result.rows.map((row) => mapLead(row as Record<string, unknown>)); },
    async getLeadById(id: string): Promise<LeadRecord | null> { const result = await pool.query(`select ${leadColumns} from operational.leads where id = $1`, [id]); return result.rows[0] ? mapLead(result.rows[0] as Record<string, unknown>) : null; },
    async lockLeadSourceIdentity(provider: string, externalId: string): Promise<void> { await pool.query('select pg_advisory_xact_lock(hashtextextended($1, 0))', [`${provider.trim()}:${externalId.trim()}`]); },
    async findLeadSourceIdentity(provider: string, externalId: string): Promise<LeadSourceIdentityRecord | null> { const result = await pool.query('select provider, external_id, lead_id, created_at, refreshed_at from operational.lead_source_identities where provider = $1 and external_id = $2', [provider.trim(), externalId.trim()]); return result.rows[0] ? mapLeadSourceIdentity(result.rows[0] as Record<string, unknown>) : null; },
    async createLeadSourceIdentity(provider: string, externalId: string, leadId: string): Promise<LeadSourceIdentityRecord> { const result = await pool.query(`insert into operational.lead_source_identities (provider, external_id, lead_id) values ($1, $2, $3) returning provider, external_id, lead_id, created_at, refreshed_at`, [provider.trim(), externalId.trim(), leadId]); return mapLeadSourceIdentity(result.rows[0] as Record<string, unknown>); },
    async findLeadByGooglePlaceId(providerPlaceId: string): Promise<LeadRecord | null> { const identity = await this.findLeadSourceIdentity('google_places', providerPlaceId); if (identity) return this.getLeadById(identity.leadId); const result = await pool.query(`select ${leadColumns} from operational.leads where source = 'google_places' and evidence @> $1::jsonb order by created_at asc limit 1`, [JSON.stringify([{ provider: 'google_places', providerPlaceId }])]); return result.rows[0] ? mapLead(result.rows[0] as Record<string, unknown>) : null; },
    async createLead(input: CreateLeadInput): Promise<LeadRecord> { const result = await pool.query(`insert into operational.leads (company_name, contact_name, contact_email, source, opportunity_summary, lead_score, evidence) values ($1, $2, $3, $4, $5, $6, $7::jsonb) returning ${leadColumns}`, [input.companyName.trim(), input.contactName?.trim() || null, input.contactEmail?.trim() || null, input.source?.trim() || null, input.opportunitySummary?.trim() || null, input.leadScore ?? null, JSON.stringify(input.evidence ?? [])]); return mapLead(result.rows[0] as Record<string, unknown>); },
    async enrichLead(id: string, expectedCompanyName: string, input: EnrichLeadInput): Promise<LeadRecord | null> { const result = await pool.query(`update operational.leads set company_name = $3, opportunity_summary = $4, evidence = $5::jsonb where id = $1 and company_name = $2 returning ${leadColumns}`, [id, expectedCompanyName.trim(), input.companyName.trim(), input.opportunitySummary?.trim() || null, JSON.stringify(input.evidence)]); return result.rows[0] ? mapLead(result.rows[0] as Record<string, unknown>) : null; },
    async updateLeadStatus(id: string, expectedStatus: string, nextStatus: string): Promise<LeadRecord | null> { const result = await pool.query(`update operational.leads set status = $3 where id = $1 and status = $2 returning ${leadColumns}`, [id, expectedStatus, nextStatus]); return result.rows[0] ? mapLead(result.rows[0] as Record<string, unknown>) : null; },
    async createPreliminaryLeadQualification(input: CreatePreliminaryLeadQualificationInput): Promise<PreliminaryLeadQualificationRecord> { const result = await pool.query(`insert into operational.lead_preliminary_qualifications (lead_id, total_score, suggested_status, human_review_required, assessments, missing_information, atlas_source_paths, actor_id) values ($1, $2, $3, true, $4::jsonb, $5::jsonb, $6::jsonb, $7) returning id, lead_id, total_score, suggested_status, human_review_required, assessments, missing_information, atlas_source_paths, actor_id, created_at`, [input.leadId, input.totalScore, input.suggestedStatus, JSON.stringify(input.assessments), JSON.stringify(input.missingInformation), JSON.stringify(input.atlasSourcePaths), input.actorId?.trim() || 'lead_agent']); return mapPreliminaryLeadQualification(result.rows[0] as Record<string, unknown>); },
    async listPreliminaryLeadQualifications(leadId: string): Promise<PreliminaryLeadQualificationRecord[]> { const result = await pool.query(`select id, lead_id, total_score, suggested_status, human_review_required, assessments, missing_information, atlas_source_paths, actor_id, created_at from operational.lead_preliminary_qualifications where lead_id = $1 order by created_at desc`, [leadId]); return result.rows.map((row) => mapPreliminaryLeadQualification(row as Record<string, unknown>)); },
    async listProjects(limit = 50): Promise<ProjectRecord[]> { const safeLimit = Math.max(1, Math.min(limit, 100)); const result = await pool.query(`select id, client_id, lead_id, name, status, service_type, created_at, updated_at from operational.projects order by created_at desc limit $1`, [safeLimit]); return result.rows.map((row) => mapProject(row as Record<string, unknown>)); },
    async getProjectById(id: string): Promise<ProjectRecord | null> { const result = await pool.query(`select id, client_id, lead_id, name, status, service_type, created_at, updated_at from operational.projects where id = $1`, [id]); return result.rows[0] ? mapProject(result.rows[0] as Record<string, unknown>) : null; },
    async createProject(input: CreateProjectInput): Promise<ProjectRecord> { const result = await pool.query(`insert into operational.projects (client_id, lead_id, name, service_type) values ($1, $2, $3, $4) returning id, client_id, lead_id, name, status, service_type, created_at, updated_at`, [input.clientId, input.leadId ?? null, input.name.trim(), input.serviceType?.trim() || 'website']); return mapProject(result.rows[0] as Record<string, unknown>); },
    async updateProjectStatus(id: string, expectedStatus: string, nextStatus: string): Promise<ProjectRecord | null> { const result = await pool.query(`update operational.projects set status = $3 where id = $1 and status = $2 returning id, client_id, lead_id, name, status, service_type, created_at, updated_at`, [id, expectedStatus, nextStatus]); return result.rows[0] ? mapProject(result.rows[0] as Record<string, unknown>) : null; },
    async listWorkflowEvents(limit = 100): Promise<WorkflowEventRecord[]> { const safeLimit = Math.max(1, Math.min(limit, 200)); const result = await pool.query(`select ${workflowEventColumns} from operational.workflow_events order by created_at desc limit $1`, [safeLimit]); return result.rows.map((row) => mapWorkflowEvent(row as Record<string, unknown>)); },
    async getWorkflowEventById(id: string): Promise<WorkflowEventRecord | null> { const result = await pool.query(`select ${workflowEventColumns} from operational.workflow_events where id = $1`, [id]); return result.rows[0] ? mapWorkflowEvent(result.rows[0] as Record<string, unknown>) : null; },
    async createWorkflowEvent(input: CreateWorkflowEventInput): Promise<WorkflowEventRecord> { const result = await pool.query(`insert into operational.workflow_events (client_id, project_id, event_type, actor_type, actor_id, payload) values ($1, $2, $3, $4, $5, $6::jsonb) returning ${workflowEventColumns}`, [input.clientId ?? null, input.projectId ?? null, input.eventType.trim(), input.actorType, input.actorId?.trim() || null, JSON.stringify(input.payload ?? {})]); return mapWorkflowEvent(result.rows[0] as Record<string, unknown>); },
  };
}
export type OperationalRepository = ReturnType<typeof createOperationalRepository>;
